spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/techpost_dev
    username: postgres # TODO: change username and password for local setup
    password: postgres # TODO: change username and password for local setup

slack:
  webhook:
    url: ${SLACK_WEBHOOK_URL:https://hooks.slack.com/services/DUMMY_URL_FOR_LOCAL}

