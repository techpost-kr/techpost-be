spring:
  config:
    import: aws-secretsmanager:prod/techpost/mysql
  datasource:
    url: jdbc:mysql://${host}:${port}/techpost
    username: ${username}
    password: ${password}

aws:
  region: ap-northeast-2

